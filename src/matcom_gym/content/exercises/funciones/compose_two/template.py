"""Composición de dos funciones.

Implementa compose(f, g) que devuelve una nueva función h tal que
h(x) == f(g(x)). Lee README.md para todas las condiciones.
"""


def compose(f, g):
    # TODO: tu código aquí
    return None


if __name__ == "__main__":
    from examples import run_examples

    run_examples(compose)
